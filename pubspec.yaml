name: fireu_frontend
description: "A new Flutter project."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ^3.10.0-290.4.beta


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.0

  # openapi clients start
  fireu_agent_client:
    path: packages/fireu_agent_client
  fireu_auth_client:
    path: packages/fireu_auth_client
  fireu_chat_client:
    path: packages/fireu_chat_client
  fireu_push_client:
    path: packages/fireu_push_client
  fireu_user_client:
    path: packages/fireu_user_client
  flutter_dotenv: ^6.0.0
  # openapi clients end

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^6.0.0


flutter:


  uses-material-design: true


